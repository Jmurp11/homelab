# Shared environment variables for both hosts
# Copy values into host-specific .env files or use with --env-file when running docker compose.
# SECURITY: Never commit real secrets to git; set permissions: chmod 600 .env.*

# TLS / Let's Encrypt
LETSENCRYPT_EMAIL=murphyj1011@gmail.com

# Shared storage paths
# NOTE: Booklore, Bookdata and Immich variables are Optiplex-only and moved to `.env.optiplex`.
# They remain in the repo for Optiplex configuration. Example file: `.env.optiplex`.

# Common ports / IDs
# BOOKLORE_PORT is Optiplex-only and moved to `.env.optiplex`.

# Timezone
TZ=UTC

# App file ownership defaults (can be overridden per-host)
APP_USER_ID=1000
APP_GROUP_ID=1000
