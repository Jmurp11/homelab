# Optiplex-specific environment variables and secrets
# Fill these with real values on the Optiplex host only. Keep this file local and secure.
# chmod 600 .env.optiplex

# Host IP (LAN)
OPTIPLEX_IP=192.168.1.50

# Vaultwarden public URL (used by Vaultwarden to generate links)
VAULTWARDEN_DOMAIN=https://vault.murphylab.app

# Vaultwarden admin token (required for admin access)
VAULTWARDEN_ADMIN_TOKEN=change-me-vaultwarden-admin-token

# Beszel (server runs on Optiplex) - server admin password (strong value)
BESZEL_ADMIN_PASSWORD=change-me-beszel-admin-password

# Beszel agent credentials for the Optiplex agent (use unique credentials per agent)
BESZEL_AGENT_TOKEN=optiplex-agent-token-change-me
BESZEL_AGENT_KEY=optiplex-agent-key-change-me

# ActualBudget admin password
ACTUALBUDGET_ADMIN_PASSWORD=change-me-actualbudget-admin-password

# Database host UID/GID used by MariaDB on Optiplex
DB_USER_ID=1000
DB_GROUP_ID=1000

# MariaDB root password and DB user (example placeholders)
MYSQL_ROOT_PASSWORD=change-me-mariadb-root
MYSQL_DATABASE=booklore
DB_USER=booklore
DB_PASSWORD=change-me-booklore-db-password

# Booklore database URL (format: mysql://user:pass@host:port/db)
DATABASE_URL=mysql://booklore:change-me-booklore-db-password@mariadb:3306/booklore

# Immich database and admin credentials (PostgreSQL)
IMMICH_DB_PASSWORD=change-me-immich-db-password
IMMICH_ADMIN_PASSWORD=change-me-immich-admin-password

# N8N workflow automation password
N8N_BASIC_AUTH_PASSWORD=change-me-n8n-password

# Portainer agent secret (shared between server and agents)
PORTAINER_AGENT_SECRET=change-me-portainer-agent-secret

# ------------------------------------------------------------------
# Optiplex-only: Booklore / OpenBooks / Immich settings
# These are intentionally placed in .env.optiplex (not shared with Pi3)
# ------------------------------------------------------------------
# Shared Book Data Path (host path where Book files are stored)
BOOKDATA_PATH=/mnt/2tb/bookdata

# Immich (server configuration)
IMMICH_VERSION=release
UPLOAD_LOCATION=/mnt/2tb/immich_uploads
DB_DATA_LOCATION=/mnt/2tb/immich_db
DB_DATABASE_NAME=immich
DB_USERNAME=immich

# Booklore specific port
BOOKLORE_PORT=6060
