# Pi3-specific environment variables and secrets
# Fill these with real values on the Pi3 host only. Keep this file local and secure.
# chmod 600 .env.pi3

# Pi3 LAN IP
PI3_IP=192.168.1.100

# Beszel agent credentials for the Pi3 agent (use unique credentials per agent)
BESZEL_AGENT_TOKEN=pi3-agent-token-change-me
BESZEL_AGENT_KEY=pi3-agent-key-change-me

# Portainer agent secret (shared with server)
PORTAINER_AGENT_SECRET=pelhamHockey11;

# If the Pi has lower UID/GID for non-root user, override
# APP_USER_ID=1000
# APP_GROUP_ID=1000

# Notes: the Pi3 runs only lightweight services; avoid placing memory/CPU intensive containers here.
